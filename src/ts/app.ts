import { XElement } from "./libs/xelement";

const layout = new XElement(document.querySelector('.layout'));

layout.addClass('laba-diena');

console.log('hello, world');

const testMessage: string = 'TypeScript works';

console.log(testMessage);
